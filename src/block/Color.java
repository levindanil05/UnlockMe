package block;

/**
 * Цвета блоков в игре.
 * Каждый блок имеет определенный цвет, красный блок является целевым.
 */
public enum Color {
    RED,
    BLUE,
    GREEN,
    YELLOW,
    ORANGE,
    PURPLE,
    GRAY,
    AQUA
}